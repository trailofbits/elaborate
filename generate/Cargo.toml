[package]
name = "generate"
version = "1.1.0"
edition = "2024"
publish = false

[dependencies]
anyhow = "1.0"
itertools = "0.14"
public-api = { version = "=0.52.1", features = [
    "experimental-feature-that-can-be-removed-in-a-patch-release_re-export-rustdoc-types",
] }
regex = "1.12"
serde_json = "1.0"

[dev-dependencies]
assert_cmd = "2.2"
similar-asserts = "2.0"

[lints]
workspace = true
